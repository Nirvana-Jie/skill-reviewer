<title>Skill 不该靠「看起来不错」发布：skill-reviewer 的证据化评审与进化方法</title>

<p><b>面向 Agent Skill 的 evidence-driven quality &amp; release governance system</b></p>
<p>它不关心 candidate 是由人、Darwin、AutoSkill、EvoSkill 还是其他 optimizer 生成；它只追问一件事：<b>在冻结的验证权威下，这个 Skill 是否真的让目标 Agent 变得更好，而且没有牺牲不可退让的能力？</b></p>

<callout emoji="💡" background-color="light-yellow" border-color="yellow">
  <p><b>一句话定义</b></p>
  <p>skill-reviewer 把 Skill 从一份提示词，变成一个需要被编译、执行、裁决和审计的外部行为系统。</p>
</callout>

<p>很多 Skill review 停在两个瞬间：读完 <code>SKILL.md</code>，觉得结构清楚；跑完一个 demo，看到输出不错。问题是，这两种信号都很容易制造错觉。</p>
<p><a href="https://arxiv.org/html/2605.23899v1">SkillLens</a> 在 150 个 domain × target × extractor 组合中观察到 <b>25% 的负迁移</b>；同一个 Skill 可能帮助一种目标模型，却伤害另一种模型。没有真实对照执行，我们无法知道所谓「写得更好」是否真的变成了「做得更好」。</p>

<hr/>

<h1>01｜先给结论：Review 的对象不是文档，而是行为变化</h1>
<p>一个可发布的 Skill 至少需要回答三类问题：</p>

<table>
  <colgroup><col width="130"/><col width="260"/><col width="420"/></colgroup>
  <thead><tr><th background-color="light-gray">证据层</th><th background-color="light-gray">关键问题</th><th background-color="light-gray">skill-reviewer 如何回答</th></tr></thead>
  <tbody>
    <tr><td><b>Package facts</b></td><td>文件、引用、脚本、权限与安装包是否完整？</td><td>严格 lint、manifest 校验、资源图与真实安装 smoke test</td></tr>
    <tr><td><b>Design hypothesis</b></td><td>触发、指令、安全边界与输出设计是否合理？</td><td>rubric 驱动的静态与语义审查，输出可证伪 finding</td></tr>
    <tr><td><b>Behavioral evidence</b></td><td>目标 Agent 在真实场景里是否更好？</td><td>candidate / baseline 配对执行、typed assertions、硬门禁与审计</td></tr>
  </tbody>
</table>

<callout emoji="📌" background-color="light-blue" border-color="blue">
  <p><b>静态审查负责发现风险，语义审查负责补充判断，真实执行负责证明效果。</b>三者是并列证据，不能用平均分互相抵消。</p>
</callout>

<p>因此，skill-reviewer 不是一个「给 Skill 打分」的工具，而是一条从 package 到 release decision 的质量链路。</p>

<hr/>

<h1>02｜一张图看懂：五个角色，只有一个发布结论</h1>
<p>架构图中的每一条边同时表达数据流和权力边界。最重要的不是模块数量，而是谁可以读、谁可以写、谁可以批准。</p>

<grid>
  <column width-ratio="0.5">
    <h2>验证与执行</h2>
    <ul>
      <li><b>Eval Authority</b>：冻结 <code>evals.json</code>、profile、baseline 与 holdout digest。</li>
      <li><b>Lead Agent</b>：把 sanitized assignment 分发给原生 worker。</li>
      <li><b>Executor</b>：只完成场景并产生产物，不读取答案，不自评发布。</li>
    </ul>
  </column>
  <column width-ratio="0.5">
    <h2>裁决与呈现</h2>
    <ul>
      <li><b>Grader / Gate</b>：确定性断言优先，语义判断补充，硬门禁最终裁决。</li>
      <li><b>Dashboard</b>：只读取证据投影，不执行、不批准，也不成为第二套真相。</li>
    </ul>
  </column>
</grid>

<p>这也解释了一个容易被误解的问题：<b>首个完整 executor 不需要绑定某种 subAgent 实现。</b>主 Agent 可以使用当前宿主提供的 worker surface 做分发；证据需要绑定 assignment、execution profile、输入与产物 digest，而不是记录某个 subAgent 的产品版本自述。</p>

<hr/>

<h1>03｜算法底座：把「更好」写成可拒绝的条件</h1>

<h2>3.1 硬门禁先于优化目标</h2>
<pre lang="text" caption="candidate 接受条件"><code>Accept(c) = all_hard_gates_pass
         ∧ every_objective_non_regressing
         ∧ at_least_one_primary_objective_materially_improves</code></pre>
<ol>
  <li seq="auto">安全、完整性、必需断言等 hard gate 必须全部通过；</li>
  <li seq="auto">每一个声明目标都不能超过容忍度退化；</li>
  <li seq="auto">至少一个主目标必须达到「实质改进」阈值；</li>
  <li seq="auto">平局、证据缺失或方向分歧都不会被包装成胜利。</li>
</ol>
<p>它与简单加权总分的区别很关键：一个漂亮的文案分数，不能抵消危险权限、丢失资源或关键能力回退。</p>

<h2>3.2 确定性断言优先，语义断言补充</h2>
<p><a href="https://arxiv.org/html/2602.12670v4">SkillsBench</a> 的实践说明，固定环境、确定性 verifier、配对条件和重复 trial 是可靠评测的基础。skill-reviewer 因此优先使用 schema、文件、退出码、精确值、集合关系和正则等 typed assertions。</p>
<p>只有无法可靠程序化的质量，才交给语义 judge。语义比较采用匿名 A/B 与调序后的 B/A 两次判断：</p>
<ul>
  <li>两次方向一致：保留为补充证据；</li>
  <li>两次方向冲突：结论为 <code>inconclusive</code>；</li>
  <li>不增加第三票强行打破分歧。</li>
</ul>
<p>这不是假设 Judge 不会犯错，而是承认它会犯错。<a href="https://arxiv.org/html/2306.05685">LLM-as-a-Judge 研究</a> 已观察到位置、冗长和自增强偏差；调序只能暴露部分不稳定，不能把 Judge 变成 Oracle。</p>

<h2>3.3 重复规则不是「多跑几次取平均」</h2>
<grid>
  <column width-ratio="0.33"><h3>确定性</h3><p><b>默认 1 次</b><br/>失败可以直接定位到确定性证据。</p></column>
  <column width-ratio="0.33"><h3>随机性</h3><p><b>成对 3 次</b><br/>candidate 与 baseline 使用相同场景和预算。</p></column>
  <column width-ratio="0.34"><h3>方向冲突</h3><p><b>不确定</b><br/>保留逐 repeat 事实，不用平均数抹平。</p></column>
</grid>

<hr/>

<h1>04｜evals.json 是真实可触发的场景协议</h1>
<p><code>evals.json</code> 不是给人阅读的样例目录，而是 runtime 的唯一行为入口。一个有效 case 同时声明：真实用户 prompt、输入文件、确定性等级、断言、优化目标、权限与数据 split。</p>
<pre lang="json" caption="可执行 eval case（节选）"><code>{
  "id": "safe-refactor",
  "split": "selection",
  "prompt": "在真实仓库中完成重构并保留验证产物",
  "determinism": "stochastic",
  "assertions": [
    { "type": "exit_code", "expected": 0, "severity": "must_pass" },
    { "type": "file_exists", "artifact": "outputs/report.md", "severity": "must_pass" }
  ],
  "objectives": [
    { "metric": "required_pass_rate", "direction": "maximize", "primary": true }
  ]
}</code></pre>
<p>编译器会把 case 展开成 <code>case × arm × repeat</code> assignments，并为每个 worker 创建独立只读 Skill / input snapshot 与可写产物目录。worker 只拿到必要输入，不会看到 assertion 的期望值、校准答案、另一 arm 的结果或最终 audit 内容。</p>

<callout emoji="❗" background-color="light-red" border-color="red">
  <p><b>无效 manifest 是发布阻断项。</b></p>
  <p>发现 <code>evals.json</code> 就意味着系统必须编译并执行其契约；字段错误、资源漂移或 digest 不一致不能静默跳过。eval 在当前授权闭环内不可变；系统可以提出改进建议，但只有用户确认后才能形成新的权威集合和新的 run。</p>
</callout>

<hr/>

<h1>05｜连续进化：让 optimizer 有创造权，但没有批准权</h1>
<p>skill-reviewer 支持受控演化，但不会让同一个角色同时改 candidate、改考试题、阅卷并宣布自己上线。</p>

<whiteboard type="mermaid">
stateDiagram-v2
    [*] --> Development
    Development --> Selection: candidate ready
    Selection --> Development: rejected / inconclusive
    Selection --> Selection: accepted parent, next round
    Selection --> Audit: stop optimizing / max 3 rounds
    Audit --> Release: one-shot pass
    Audit --> Blocked: fail, no feedback to optimizer
    Release --> [*]
    Blocked --> [*]
</whiteboard>

<ul>
  <li>development surrogate 可以学习失败、提议新断言、帮助生成下一 candidate；</li>
  <li>selection 权威在一次闭环里冻结，候选必须绑定已接受父代；</li>
  <li>rejected candidate 永远不能成为下一轮父代；</li>
  <li>最终 opaque audit 只运行一次，失败细节不回流 optimizer；</li>
  <li>默认最多 3 轮 selection，不把验证集训练成开发集。</li>
</ul>
<p>Skill 本身允许大幅架构调整，而不仅是小段文本 patch。若目录拓扑、模块职责或执行面变化已经破坏同源比较，系统会重置 continuity 和 baseline，而不是用一个旧版本号伪装成同一条 lineage。</p>
<p><a href="https://arxiv.org/html/2604.01687v2">CoEvoSkills</a> 证明了开发期 surrogate verifier 可以随失败共同进化，也展示了 surrogate 全部通过而隐藏 oracle 仍失败的情况。我们的取舍更保守：<b>代理测试可以成长，但不能接管最终权威。</b></p>

<hr/>

<h1>06｜Dashboard 不是报告皮肤，而是验证链路的产品界面</h1>
<p>当前 Dashboard 使用 React、TypeScript、Vite、Vitest 与 <code>@pierre/diffs</code> 构建，是一个只读 evidence workbench：</p>
<ul>
  <li>左侧定位 case、gate、assertion 与 artifact；</li>
  <li>中间查看证据或 candidate / baseline 文档 diff；</li>
  <li>右侧检查 run、profile、digest、repeat、lineage 与限制；</li>
  <li>支持中英文、明暗主题、split / unified diff、自动换行、focus mode、命令面板与固定链接；</li>
  <li>大文件按需加载，diff 使用 worker 高亮与虚拟化渲染；</li>
  <li>刷新失败时保留最后一次已验证投影，并明确标记 stale state。</li>
</ul>
<callout emoji="📝" background-color="light-gray" border-color="gray">
  <p>Dashboard 展示的是同一证据链的只读投影。截图能证明信息结构和交互，不能单独证明算法收益；发布结论仍要回到 retained JSON、artifact 与 gate decision。</p>
</callout>

<hr/>

<h1>07｜一次真实运行：系统最有价值的回答可能是「不通过」</h1>
<p>为了让截图不是 mock，我们用当前 candidate 与 <code>main</code> 上的 accepted baseline 编译了一次新的 selection run：<code>run-a324c00268e0228b50e3</code>。</p>
<grid>
  <column width-ratio="0.25"><h3>场景</h3><p><b>2</b><br/>公开 selection</p></column>
  <column width-ratio="0.25"><h3>真实执行</h3><p><b>8</b><br/>两个隔离 arm</p></column>
  <column width-ratio="0.25"><h3>硬门禁</h3><p><b>5 / 6</b><br/>一个阻断</p></column>
  <column width-ratio="0.25"><h3>最终结论</h3><p><b>inconclusive</b><br/>candidate rejected</p></column>
</grid>
<ul>
  <li>1 个确定性 case 各执行 1 次，1 个随机性 case 各执行 3 次，共 8 次原生 worker execution；</li>
  <li>worker 只看到 sanitized assignment、冻结 Skill snapshot 与声明输入，没有看到 assertion 期望值、另一 arm 输出或发布结论；</li>
  <li>确定性 grading 后，唯一的 semantic pair 由两个独立 blind grader 做 A/B 与 B/A 调序；匿名结果为 <code>B</code> 与 <code>A</code>，映射后两次都偏好 <code>old_skill</code>。</li>
</ul>
<table>
  <colgroup><col width="320"/><col width="150"/><col width="150"/><col width="330"/></colgroup>
  <thead><tr><th background-color="light-gray">证据</th><th background-color="light-gray">candidate</th><th background-color="light-gray">baseline</th><th background-color="light-gray">结论</th></tr></thead>
  <tbody>
    <tr><td><code>missing-baseline-is-inconclusive</code> required pass rate</td><td>0.75</td><td>0.75</td><td>candidate hard gate 失败</td></tr>
    <tr><td><code>ready-skill-calibration</code> required pass rate</td><td>1.00</td><td>1.00</td><td>两侧确定性断言全部通过</td></tr>
    <tr><td>blind semantic pair</td><td>—</td><td>preferred</td><td>两次调序映射后均偏好 baseline</td></tr>
    <tr><td>primary objective delta</td><td>0</td><td>0</td><td>没有实质提升</td></tr>
  </tbody>
</table>
<callout emoji="❌" background-color="light-red" border-color="red">
  <p><b>真实结论：6 个硬门禁只通过 5 个，selection decision 为 inconclusive。</b></p>
  <p>candidate 被记录为 rejected，不能成为下一父代。这个 run 使用公开 calibration 数据，<code>release_eligible=false</code>，没有冒充一次 opaque final audit。</p>
</callout>
<p>一条有效验证链路不以「candidate 一定获胜」为成功标准。真实 run 若发现 candidate 没有实质提升、某一 repeat 方向冲突、semantic judge 不稳定，或者 audit 阻断发布，这恰恰证明治理层没有为了展示效果而篡改结论。</p>

<hr/>

<h1>08｜与 Darwin、skill-evolve、SkillEvo、EvoSkill 怎么比较</h1>
<p>「skill-evo」不是唯一项目。为了避免混淆，本文区分 <a href="https://github.com/OrangeViolin/skill-evolve">OrangeViolin skill-evolve</a>、<a href="https://github.com/ECNU-ICALK/AutoSkill/tree/main/SkillEvo">AutoSkill / SkillEvo</a> 与 <a href="https://github.com/sentient-agi/EvoSkill">Sentient EvoSkill</a>。它们和 <a href="https://github.com/alchaincyf/darwin-skill">Darwin Skill</a> 解决的是相邻但不同的问题。</p>

<table>
  <colgroup><col width="140"/><col width="220"/><col width="300"/><col width="340"/></colgroup>
  <thead><tr><th background-color="light-gray">系统</th><th background-color="light-gray">最擅长回答</th><th background-color="light-gray">主要优点</th><th background-color="light-gray">公开实现中的边界</th></tr></thead>
  <tbody>
    <tr><td><b>skill-reviewer</b></td><td>这个多文件 Skill 为什么可以或不可以发布？</td><td>冻结 eval 权威、原生 Agent 配对、typed assertions、硬门禁、一次 audit、证据 Dashboard</td><td>不是长期 replay daemon；尚无跨系统共同 benchmark</td></tr>
    <tr><td><b>Darwin Skill</b></td><td>如何低成本把一个文本 Skill 迭代得更好？</td><td>用户确认测试 Prompt、with/baseline 子 Agent、Git 回退、体验轻</td><td>可降级 dry-run；加权总分可能掩盖单维退化；包级治理较弱</td></tr>
    <tr><td><b>skill-evolve</b></td><td>如何有步骤地反思和改写 Skill？</td><td>OTF / JIT / Bootstrap 方法简单，适合人工协作</td><td>公开仓库未提供 eval runner 或独立证据台</td></tr>
    <tr><td><b>AutoSkill / SkillEvo</b></td><td>如何从长期 replay 中持续晋级 champion？</td><td>replay pool、dev mutation、promotion test、provenance</td><td>当前模块仍是 MVP；规则受 baseline / 历史启发，未自动回写主 SkillBank</td></tr>
    <tr><td><b>Sentient EvoSkill</b></td><td>如何在明确 benchmark 上搜索高分 skill program？</td><td>多角色分工、Git branch、frontier、公开论文结果</td><td>当前 frontier 是标量 top-K；缺少包级 release hard gates 与一次 opaque audit</td></tr>
  </tbody>
</table>

<callout emoji="✅" background-color="light-green" border-color="green">
  <p><b>不存在脱离场景的「谁全面更好」。</b></p>
  <ul>
    <li>个人快速改写一个指令型 Skill：Darwin 更轻；</li>
    <li>从长期使用历史持续学习：AutoSkill / SkillEvo 更原生；</li>
    <li>有稳定 benchmark，要搜索 program：Sentient EvoSkill 更完整；</li>
    <li>多文件 Skill 要进入生产，必须回答「为什么能发」：skill-reviewer 的治理链路更匹配。</li>
  </ul>
</callout>
<p>最合理的组合不是互斥，而是让 Darwin、AutoSkill、EvoSkill、GEPA 等系统负责<b>提出 candidate</b>，再由 skill-reviewer 在统一冻结权威下负责<b>发布裁决</b>。</p>

<hr/>

<h1>09｜论文如何真正落到设计里</h1>
<table>
  <colgroup><col width="150"/><col width="340"/><col width="470"/></colgroup>
  <thead><tr><th background-color="light-gray">研究</th><th background-color="light-gray">关键发现</th><th background-color="light-gray">在 skill-reviewer 中的落点</th></tr></thead>
  <tbody>
    <tr><td><a href="https://arxiv.org/html/2605.23899v1">SkillLens</a></td><td>文本质量不等于下游 utility；25% 组合负迁移</td><td>with / no-skill 或 old-skill 配对；rubric 只作假设，行为结果才是效用证据</td></tr>
    <tr><td><a href="https://arxiv.org/html/2602.12670v4">SkillsBench</a></td><td>确定性 verifier、fresh env、重复 trial 与泄漏治理</td><td>typed assertion 优先、独立 workspace、成对重复、逐 profile 展示</td></tr>
    <tr><td><a href="https://arxiv.org/html/2605.23904v2">SkillOpt</a></td><td>冻结 harness、held-out strict improvement、rejected buffer</td><td>权威冻结、严格提升、失败候选不继承；架构重写时额外 continuity reset</td></tr>
    <tr><td><a href="https://arxiv.org/html/2604.01687v2">CoEvoSkills</a></td><td>surrogate 可以共进化，但不能替代隐藏 oracle</td><td>development suggestion 与 selection / audit authority 分离</td></tr>
    <tr><td><a href="https://arxiv.org/abs/2507.19457">GEPA</a></td><td>Genetic-Pareto 有利于探索多样性</td><td>可用于未来多父代搜索；发布门禁仍坚持每个关键目标不退化</td></tr>
  </tbody>
</table>
<p>尤其需要避免一个误读：GEPA 的 Pareto frontier 用于保留在不同样本上有优势的候选；skill-reviewer 的 Pareto 约束用于禁止发布目标回退。它们都叫 Pareto，但职责不同。</p>

<hr/>

<h1>10｜使用方式：从一分钟静态审查到完整发布验证</h1>
<h2>安装</h2>
<pre lang="bash"><code>npx skills add Nirvana-Jie/skill-reviewer --skill skill-reviewer</code></pre>
<p>安装包会携带 <code>SKILL.md</code>、scripts、references、eval fixtures 与可直接运行的 Dashboard 静态资源，不依赖仓库根目录的兼容副本。</p>

<h2>路径 A：静态 / 设计审查</h2>
<p>直接让 Agent 使用 skill-reviewer 审查目标 Skill；如果明确说「只做静态审查，不运行 eval」，输出会把行为验证标记为 <code>not-run</code>，不会伪造 runtime 结论。</p>

<h2>路径 B：执行已有 evals.json</h2>
<pre lang="bash"><code>python3 skills/skill-reviewer/scripts/skill_eval_runtime.py compile \
  --manifest &lt;skill&gt;/evals/evals.json \
  --subject &lt;candidate&gt; \
  --execution-profile &lt;profile.json&gt; \
  --baseline-kind old_skill \
  --baseline-path &lt;accepted-skill&gt; \
  --split selection \
  --workspace /tmp/skill-reviewer-run</code></pre>
<p>主 Agent 随后分发 assignment，收集 artifact，运行 <code>grade</code> 与 <code>decide</code>。不要把 worker completion 当作通过；只有绑定且通过的断言证据才能改变 verification level。</p>

<h2>路径 C：查看证据 Dashboard</h2>
<pre lang="bash"><code>python3 skills/skill-reviewer/scripts/skill_eval_runtime.py project-dashboard \
  --workspace /tmp/skill-reviewer-run \
  --state /tmp/skill-reviewer-control/evolution-state.json \
  --output /tmp/skill-reviewer-run/dashboard-data.json

python3 skills/skill-reviewer/scripts/serve_skill_dashboard.py \
  --workspace /tmp/skill-reviewer-run</code></pre>

<hr/>

<h1>11｜最佳实践：让证据比结论更难伪造</h1>
<ol>
  <li seq="auto"><b>先冻结权威，再启动 worker。</b> baseline、manifest、profile、fixture 与 holdout 都要绑定 digest。</li>
  <li seq="auto"><b>同一场景配对、同一轮分发。</b> candidate 与 baseline 使用相同输入和预算，各自在 fresh workspace 中执行。</li>
  <li seq="auto"><b>把 executor、grader、optimizer 分开。</b> executor 不能自评，optimizer 不能改最终 oracle，Dashboard 不能批准发布。</li>
  <li seq="auto"><b>确定性能程序化就不要交给 LLM。</b>语义 judge 只负责无法可靠编码的补充质量。</li>
  <li seq="auto"><b>允许 inconclusive。</b>缺 baseline、丢 artifact、调序冲突、repeat 方向分歧时，诚实地停止推断。</li>
  <li seq="auto"><b>eval 修改需要用户确认。</b>系统可以建议新增场景，但不能为了让 candidate 通过而静默改题。</li>
  <li seq="auto"><b>区分 selection 与 audit。</b>selection 服务有限搜索；最终 audit 一次性、不可见、失败不回流。</li>
  <li seq="auto"><b>架构变化就重置比较连续性。</b>大改不是问题，假装仍与旧 baseline 完全同源才是问题。</li>
  <li seq="auto"><b>展示逐场景事实，不只展示总分。</b>把 case、arm、repeat、artifact、diff 与 gate 放在同一导航脊柱上。</li>
  <li seq="auto"><b>把发布治理与运行时防护组合。</b>package review 不能替代 sandbox、权限系统和调用时风险拦截。</li>
</ol>

<hr/>

<h1>12｜边界与下一步</h1>
<p>skill-reviewer 当前实现的是有界、可审计的 candidate evolution 与 release governance，不是永久监听生产 replay 的自治学习 daemon；native worker 的真实工具隔离也仍需要宿主 Agent / harness 执行。公开 fixture 用于校准协议，不能替代组织自己的 opaque holdout。</p>
<ul>
  <li>引入 GEPA 式多父代与 ancestry，提高探索多样性，但不放松发布门禁；</li>
  <li>接入 AutoSkill 类 replay source，把长期经验变成 development candidate；</li>
  <li>把开发期 surrogate assertion 建议做成独立审批流；</li>
  <li>将本地 digest journal 锚定到外部 append-only transparency log；</li>
  <li>扩展跨模型、跨 harness 的 profile matrix，系统识别 target-specific 负迁移；</li>
  <li>与 STARS 类 invocation-time 风险控制组合，覆盖「安全地发布」和「安全地调用」。</li>
</ul>

<callout emoji="🏁" background-color="light-green" border-color="green">
  <p><b>Skill Reviewer 是 Agent Skill 的 evidence-driven quality and release governance system。</b></p>
  <p>它不承诺每次都把 Skill 改得更好；它承诺任何「更好」都必须经过真实执行、保守裁决和可追溯证据。</p>
</callout>

<hr/>

<h1>延伸阅读与致谢</h1>
<ul>
  <li><a href="https://arxiv.org/html/2605.23899v1">SkillLens：From Raw Experience to Skill Consumption</a></li>
  <li><a href="https://arxiv.org/html/2602.12670v4">SkillsBench</a></li>
  <li><a href="https://arxiv.org/html/2605.23904v2">SkillOpt</a></li>
  <li><a href="https://arxiv.org/html/2604.01687v2">CoEvoSkills</a></li>
</ul>
<p>特别感谢 <a href="https://bytetech.info/articles/7646993990063783946?from=message_bot&amp;sender_type=101&amp;message_id=7649212900263706662&amp;column=like#doxcnoPJmlIa00RtCdca0OyXy3f">ByteTech 文章《从玄学调 Prompt 到科学评测，我只用了一个 skill》</a> 对自进化架构与评测表达方式的启发。本文借鉴了其「问题—实验—结论」的叙事与视觉节奏；本文中的系统结论、对比边界和实现事实均基于 skill-reviewer 当前代码与一手论文重新核验。</p>
